# Binaries (anchored to repo root and cmd/archivist/ so the cmd/archivist/
# source directory itself is not ignored)
/archivist
/archivist.exe
/cmd/archivist/archivist
/cmd/archivist/archivist.exe

# Goreleaser
/dist/

# Test + coverage
*.test
*.out
coverage.out
coverage.html

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS noise
.DS_Store
.AppleDouble
.LSOverride

# Local env (covers .env, .env.local, .env.production, etc. — keep .env*.example committable)
.env
.env.*
!.env*.example
.envrc

# JS deps (defense — no JS in this repo today, but blocks accidental adds)
node_modules/
